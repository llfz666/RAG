# 📊 Modular RAG MCP Server · 复习完成报告

**复习时间**：2026 年 3 月 16 日  
**复习章节**：第 5 章 - 第 9 章（共 5 章）  
**总题数**：27 题

---

## 📈 总体掌握情况

| 章节 | 题目数 | 优秀 | 需加强 | 综合评分 |
|------|--------|------|--------|---------|
| 第 5 章：可插拔架构设计 | 6 题 | 6 | 0 | ⭐⭐⭐⭐⭐ |
| 第 6 章：可观测性与 Dashboard | 6 题 | 6 | 0 | ⭐⭐⭐⭐⭐ |
| 第 7 章：评估体系 | 5 题 | 5 | 0 | ⭐⭐⭐⭐⭐ |
| 第 8 章：测试体系与工程质量 | 5 题 | 3 | 2 | ⭐⭐⭐⭐ |
| 第 9 章：存储与持久化架构 | 5 题 | 3 | 2 | ⭐⭐⭐⭐ |
| **总计** | **27 题** | **23** | **4** | **⭐⭐⭐⭐⭐** |

---

## ✅ 掌握优秀的知识点

### 第 5 章：可插拔架构设计（6/6 优秀）
- LLM Factory 的核心价值与实现机制
- 可插拔架构的核心设计原则
- Factory 模式的"热切换"机制
- 配置驱动的实现细节
- 多模态组件的适配方式
- 可插拔架构的边界与局限性

### 第 6 章：可观测性与 Dashboard（6/6 优秀）
- Trace 分类（Ingestion/Query）
- Query Trace 5 阶段详解
- Ingestion Trace 5 阶段详解
- Dashboard 6 个功能页面
- 动态渲染机制
- 全链路白盒化的核心价值

### 第 7 章：评估体系（5/5 优秀）
- Hit Rate@K 指标与含义
- MRR 指标与 Hit Rate 的区别
- Faithfulness vs Answer Relevancy
- 评估框架（Ragas/DeepEval/自定义）
- 评估闭环的核心价值

### 第 8 章：测试体系与工程质量（3/5 优秀）
- 测试分层（Unit/Integration/E2E）
- 集成测试的定位与职责
- conftest.py 的作用

### 第 9 章：存储与持久化架构（3/5 优秀）
- SQLite 选型理由
- ChunkID 生成算法
- BM25 持久化方案与升级路径

---

## ⚠️ 需要加强的知识点

### 第 8 章：测试体系与工程质量

#### 8A-02：BM25 Roundtrip 测试
**问题**：为什么 BM25 要有"roundtrip"测试？名字意味着什么？

**完整答案**：
"Roundtrip" 意为"**往返旅程**"，在本项目中特指验证 BM25 索引的完整数据往返路径：
1. **写入**：将文档写入 BM25 索引
2. **保存**：将索引持久化到磁盘（pickle 文件）
3. **加载**：从磁盘重新加载索引
4. **查询**：执行查询，验证结果正确

**测试目的**：验证序列化/反序列化没有数据丢失或精度损失，确保索引重启后仍能正常工作。

---

#### 8A-05：Dashboard Smoke Test
**问题**：E2E 测试中的 Dashboard Smoke Test 是如何在不启动真实服务器的情况下测试的？

**完整答案**：
项目使用 **Streamlit AppTest**（无头渲染）来测试 Dashboard：
1. **AppTest 是什么**：Streamlit 官方提供的测试工具，可以在**进程内运行** Streamlit 应用逻辑
2. **无需启动服务器**：直接在测试代码中导入并运行 Dashboard 页面
3. **测试方式**：
```python
from streamlit.testing.v1 import AppTest

def test_dashboard_smoke():
    at = AppTest.from_file("dashboard/app.py")
    at.run()
    assert not at.exception  # 验证无异常
```

---

### 第 9 章：存储与持久化架构

#### 9A-02：WAL 并发机制
**问题**：WAL（Write-Ahead Logging）在 SQLite 中如何保证并发安全？

**完整答案**：
**传统 SQLite 模式**：写操作需要独占锁，读写互斥。

**WAL 模式**：
1. **写入流程**：写操作先追加到 WAL 日志文件（顺序写入），然后异步合并到主数据库文件
2. **读取流程**：读操作同时读主数据库文件 + WAL 日志，合并得到最新数据
3. **并发模型**：允许多个 Reader 同时读，允许一个 Writer 写，读写互不阻塞
4. **崩溃恢复**：系统崩溃后，SQLite 自动重放 WAL 日志中已提交的事务

---

#### 9A-05：删除操作一致性
**问题**：删除文档时，系统如何保证 SQLite、Chroma、BM25 三处数据的一致性？

**完整答案**：
**当前实现**：
1. **事务性删除**：在 `DocumentManager.delete_document()` 中按顺序删除：
   - 先删除 SQLite 中的文档元数据（事务性）
   - 再删除 Chroma 中的向量
   - 最后删除 BM25 索引中的数据
2. **异常处理**：如果任一步骤失败，抛出异常并回滚 SQLite 事务

**局限性**：当前没有分布式事务机制，极端情况下可能出现不一致。

**改进方向**：补偿事务、定期校验。

---

## 💡 老师点评

本次复习表现非常出色！27 道题中 23 道回答优秀，整体掌握程度达到 85%。

**亮点**：
- 能够用生动的比喻解释复杂概念（如"自行车 vs 卡车"、"家庭住址"、"飞行模拟器"等）
- 对架构设计层面的理解非常深入（可插拔、可观测性、评估体系）
- 能够将知识点串联起来，形成系统化的认知

**建议加强**：
- 底层技术细节的理解（WAL 机制、序列化测试）
- 分布式一致性问题的深入思考

---

## 📚 后续建议

1. **回顾需加强的 4 道题**：花 10 分钟重新阅读 8A-02、8A-05、9A-02、9A-05 的完整答案
2. **实战演练**：尝试向他人讲解这些概念，检验是否真正内化
3. **模拟面试**：可以切换到"模拟面试"模式，进行更深度的追问练习

---

**复习进度已保存至**：`interview_report_20260316_2212.md`

🎉 祝你面试顺利！